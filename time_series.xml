<?xml version="1.0" encoding="UTF-8"?>
<manifest>
   <remote name="origin" fetch="ssh://git@gitlab.devtools.intel.com:29418/Indu/edge-insights-industrial" />
   <default revision="master" remote="origin" sync-j="4" />

   <!-- EII core services with timeseries use case -->
   <include name="include/base_time_series.xml" />

   <!-- Start: Optional EII services for timeseries use case (Comment the ones not required) -->
   <project name="eii-opcua-export" path="IEdgeInsights/OpcuaExport" />
   <project name="eii-rest-data-export" path="IEdgeInsights/RestDataExport" />
   <project name="eii-zmq-broker" path="IEdgeInsights/ZmqBroker" />
   <!-- End: Optional EII services for timeseries use case (Comment the ones not required) -->
   
   <!-- Start: EII Ingredient components -->
   <project name="non-opensource/ingredients/manageability" path="manageability" />
   <!-- End: EII Ingredient components -->

</manifest>
